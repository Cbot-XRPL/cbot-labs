# Example environment variables for the XRPL Trading Bot workspace
# Do NOT store secrets in the repository. Use your secret manager to inject values.
PORT=4002
OWNER_WALLETS=rVeHp61gJ9MxoqMkAMmEbow8KYWk3AH1X
# Optional: for XRPL connectivity and server-side signing (seeds must never be committed)
XRPL_WALLET_SEED=your_xrpl_seed_here
RIPPLED_URL=wss://s.altnet.rippletest.net:51233
# Optional: set NODE_ENV=development or production

